- breadcrumbs_val = "?previous_page_title=Recent&return_url=#{admin_library_path}?tab=recent"

- if assets.blank?
  %ul.assetList
    %li
- else
  %table{:id => 'assets_table', :cellpadding => 0, :cellspacing => 0, :rel => "" , :summary => "Assets" }
    %thead
      %tr
        %th
        %th Title
        %th.author File Type
        %th.author Author
        %th.date Uploaded
        %th.date Updated
    %tbody
      - for asset in assets
        %tr
          %td.thumbnail
            %a{:href => admin_asset_url(asset)+breadcrumbs_val, :class => "assetLink"}
              %img{:src => asset.thumb_small_url  }
          %td
            %span.asset_title
              = asset.name
            %span.controls
              %a{:href => admin_asset_url(asset)+breadcrumbs_val, :class => "assetLink"}
                View
              %a{:href => edit_admin_asset_url(asset)+breadcrumbs_val, :class => "assetLink"}
                Edit  
              %a{:href => crop_admin_asset_url(asset)+breadcrumbs_val, :class => "assetLink"}
                Crop  
          %td.author
            = asset.asset_type.name      
          %td.author
            = asset.user.full_name
          %td.date
            = date_format(asset.created_at)
          %td.date
            = date_format(asset.updated_at)